// Root component that serves as the entry point for the application
// Keeps the top level clean by delegating layout to AppContainer
import AppContainer from './AppContainer';

export default () => <AppContainer />; 